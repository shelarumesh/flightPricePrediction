version: '10.0'
services:

  sleek-airflow:

    image: airflow-image:latest

    volumes:

      - ./airflow:/opt/airflow
      - ./logs:/opt/airflow/models
      - ./data:/opt/data

    ports:

      - "8080:8080"

    command: airflow standalone